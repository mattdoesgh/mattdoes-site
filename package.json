{
  "name": "mattdoes-site",
  "version": "0.1.0",
  "private": true,
  "description": "mattdoes.online — Obsidian vault → static HTML",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "clean": "rm -rf dist",
    "lint": "html-validate \"dist/**/*.html\"",
    "audit": "npm audit --audit-level=moderate",
    "pretest": "VAULT_DIR=test/fixture-vault SITE_URL=https://mattdoes.online node build.js",
    "test": "node --test \"test/**/*.test.js\"",
    "deploy:workers": "cd workers/listening && npx wrangler deploy && cd ../geo && npx wrangler deploy",
    "bake-geo": "node scripts/bake-home-geojson.js",
    "optimize-media": "node scripts/optimize-media.js",
    "sync-media": "node --env-file-if-exists=.env scripts/sync-media.js"
  },
  "dependencies": {
    "marked": "^18.0.5",
    "marked-footnote": "^1.4.0",
    "sanitize-html": "^2.17.5",
    "shiki": "^4.2.0",
    "yaml": "^2.9.0"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "axe-core": "^4.12.0",
    "culori": "^4.0.2",
    "html-validate": "^11.5.3",
    "jsdom": "^29.1.1",
    "lightningcss": "^1.32.0",
    "sharp": "^0.34.5",
    "terser": "^5.48.0"
  }
}
