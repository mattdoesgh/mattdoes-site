# Cloudflare Pages — URL redirects
# Docs: https://developers.cloudflare.com/pages/configuration/redirects/

# Apex canonicalization (handled at DNS + Pages custom domains, but belt-and-braces)
https://www.mattdoes.online/*  https://mattdoes.online/:splat  301

# Legacy paths from the pre-rebuild site (fill in as needed)
# /old-path                    /new-path                        301

# Blog consolidation — /blog/ is the unified journal + making + thoughts
# view with client-side kind chips. /journal/, /making/, and /thoughts/ are
# now REAL server-generated archive index pages (see build.js), so they no
# longer redirect — a scripts-disabled visitor gets the actual section
# content. Individual post URLs (e.g. /journal/<slug>/) are unchanged.

# RSS alias
/rss.xml                       /feed.xml                        301
/atom.xml                      /feed.xml                        301
